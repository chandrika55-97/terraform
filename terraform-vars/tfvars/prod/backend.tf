bucket = "state-prod-practice"
    key    = "remote-state-demo" #every repo should have unique key
    region = "us-east-1"
    dynamodb_table = "table_dynamo-prod"

    #here different bucket names so key value is same also no problem
     